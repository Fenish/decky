# 2026-09-10

## Decky desktop and UI refinement

Rewrote the desktop around validated shared configuration, native action execution, a typed preload and feature folders. Added all requested action categories, nested pages, independent toggle artwork, image adjustments, profile recovery and a compatible firmware v2 page cache. Detailed earlier hardware results are in [[decky-desktop]].

The user selected smooth obsidian disconnected artwork, automatic connection retries and a camera transition into the center key. They then selected the floating-grid dashboard with a right editor that appears when selecting a key. Implemented the contextual action picker, compact editors, pages/settings panels and icon dock. Removed old verbose screens.

Follow-up revisions: custom draggable window chrome; tightly fitted white vector logo with transparent app icons; optional labels with centered icons; separate OFF/ON background colors. Tests and build details are in [[decky-desktop]] and desktop/README.md. Generated concepts are retained in desktop/design-concepts/.

Later requests completed: installed-app search with native icons, full Lucide search, independent/visible/background scripts and a 30-second default timeout, duplicate/move/swap keys, accent labels, hover image removal, adjustable centered icon/text gap, and matching live canvas previews. Firmware v3 now preloads configured pages with a centered logo/progress screen and detects application loss. Hardware verified eight pages remain cached even after all are updated; cached switches measured 78–84 ms. BYE and 12-second heartbeat timeout both display Disconnected. Source/build and exact hardware evidence are recorded in [[decky-desktop]]. Avoid repeated full builds and leave launching the app to the user.

Fixed the reported freeze when alternating Mute/Deafen: firmware v4 caches ON artwork per key and uses small STATE commands instead of page snapshots. Both desktop main process and renderer now keep runtime toggle changes out of image sync. Flashed and measured 100 alternating changes on the panel, mean 11.72 ms, no image uploads. Actual framebuffer verified both ON states simultaneously. Regression tests and build passed; details in [[decky-desktop]].

Added wireless setup in Settings and flashed firmware v5. USB provisions Wi-Fi and a paired PC credential; the device saves successful credentials and reconnects, while authenticated TCP carries the existing protocol. Real Wi-Fi scan passed; local socket and UI tests passed. User chooses the actual network/password in Settings and clicks Use Wi-Fi. Details and verification limits in [[decky-desktop]].

After Wi-Fi initialization caused physical LCD drift, added vertical-blank scanout recovery around Wi-Fi/NVS work without changing geometry or timings. Flashed and checked scanning/recovery; user confirmed the screen alignment is fixed.

Persistent wireless boot: firmware v6 now keeps sparse page artwork and both toggle appearances on the existing microSD card, and negotiates 4 KB Wi-Fi blocks. Saved user's actual profile over Wi-Fi, reset the device, verified zero image uploads and 1.569-second SD restoration; total reset-to-ready 9.714 seconds. Details in decky-desktop memory and desktop/output/persistent-cache-check.json. Card was not formatted and unrelated files were untouched.

## Firmware from the app, encrypted Wi-Fi, USB first

The desktop app now installs the deck's firmware itself: Settings → Firmware for updates, and the Disconnected page for a board still on factory firmware. esptool-js was proven on the real board before anything depended on it - ESP32-S3 rev 0.2, flash ID 0x1640c8, four images written and MD5-verified at 460800 baud in 20.8 s. That test found two faults that would have made healthy decks look dead: `serialport`'s `set()` asserts any line left out, and esptool-js 0.6.1's `after("hard_reset")` never resets the chip. Both are handled; the user then installed the protocol-7 firmware from Settings on hardware. Decisions: [[2026-09-10-desktop-flasher]].

Wi-Fi traffic after pairing is AES-128-GCM framed, protocol 7, and the desktop refuses plain-text firmware over Wi-Fi. Covered by framing tests and a fake deck; against the real firmware it still awaits the deck losing its cable while powered. Decisions: [[2026-09-10-encrypted-wifi]].

The connection rule changed several times in one afternoon before settling on USB first, Wi-Fi only without a cable, no manual switch. Decisions: [[2026-09-10-usb-first]].

Also: the CH340 wedged mid-session (Windows error 31 on open) and needed a replug; the Firmware section kept its "installed, restarting" message after the deck came back, fixed by keying it to the deck's firmware; flash-layout wording claimed artwork lived in the flash data partition, when it is on the microSD card.

Moved here from the desktop README, which keeps no measurements: the user's three-page profile saved over Wi-Fi in 8.805 s (21 images, 609,588 payload bytes); after a hardware reset all pages and both ON images loaded from SD in 1.569 s with zero uploads; reset through Wi-Fi reconnection to ready took 9.714 s.

Open: encrypted Wi-Fi on the real firmware; the two release workflows, untested until the GitHub repository exists; the user's question about automatic version numbers.

Settings got a Forget button under Wireless: USB only, asks first, clears the deck's saved network and this PC's pairing file for that deck. The firmware already had WIFI_FORGET; only the desktop was missing the button. Not yet tried on the real deck.

The app, taskbar, installer and tray icon now come from `Branding/logo-app.png` (the thin-stroke mark), trimmed of its ~18% transparent margin so it fills the icon like the old bold one did. The window and tray load the multi-size `icon.ico` instead of the 512 px PNG. The 16 px tray size is soft because the strokes land under 1 px; on a light taskbar the white mark barely shows, which was also true of the old icon. The title-bar logo is unchanged.

Wireless settings, per the user: Forget shows only once the deck is connected (it had appeared while still joining, because the firmware reports the joining network's name), and a successful join closes the network list and password form. The "SD cache ready" line was removed; the no-card warning stays.

Unplugging the deck left the app on the dashboard, and it never fell back to Wi-Fi (the user's power-bank test). Cause, measured on the deck with a script using the app's own `DeckLink`: the serial library closed the port about 1 s after the unplug, but the link kept the dead port object, and the next heartbeat PING's write waited forever. That stalled the single serial queue, so status checks and reconnects never ran again. Fix: drop the port on `close` and refuse writes to a closed port; a unit test reproduces the stall with a fake port. The user confirmed the Disconnected page and the Wi-Fi fallback both work after the fix. The deck was reachable over Wi-Fi throughout (discovery reply and `aesgcm1` challenge at 192.168.1.20).
